{erl_opts, [debug_info, {d, 'TEST', true}]}.

%% == Dependencies ==
% Where to put any downloaded depandencies. Default is `deps'
{deps_dir, ["deps"]}.
{deps, [{rec, ".*",
        {git, "git://github.com/devR2/rec.git", {branch, "master"}}}
]}.
